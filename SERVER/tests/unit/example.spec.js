const controller = require('../../controllers/post.controller');

describe('post.controller.js', () => {
  it('should create a post when createPost called', () => {
    // test code here
    controller.createPost();
  })
})
